# Agent Guidelines

- Run `npm run check` before submitting changes that touch the Astro source files under `src/`.
- Prefer Markdown headings that increase by a single level at a time in documentation updates.
